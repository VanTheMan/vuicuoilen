<%= form_for @post do |f| %>
	<div class="field">
		<%= f.text_area :content, placeholder: "Share your VCL...", rows: 4, cols: 30 %>
	</div>
	<div class="submit">
		<%= f.submit "Post" %>
	</div>
<% end %>